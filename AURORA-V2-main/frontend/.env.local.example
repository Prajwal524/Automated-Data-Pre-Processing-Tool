# Copy to .env.local for local development.
# On Render, set NEXT_PUBLIC_API_URL as an Environment Variable on the
# frontend service to your deployed backend URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
